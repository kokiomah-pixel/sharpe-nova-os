# Continuous Decision Loop

All systems must operate as:

intent -> size -> Nova -> decision_status -> execution (only if admitted)

## Example

1. Agent proposes allocation
2. System calls Nova
3. Nova returns decision_status
4. System enforces outcome
5. Proof is stored

This loop must occur before EVERY capital-moving action.
